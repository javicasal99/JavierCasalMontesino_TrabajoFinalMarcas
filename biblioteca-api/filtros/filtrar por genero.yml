info:
  name: filtrar por genero
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/libros/buscar?genero=Fantasía
  params:
    - name: genero
      value: Fantasía
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
