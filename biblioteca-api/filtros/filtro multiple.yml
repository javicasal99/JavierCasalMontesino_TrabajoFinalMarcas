info:
  name: filtro multiple
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/libros/buscar?disponible=true&ordenar_por=valoracion&orden=desc
  params:
    - name: disponible
      value: "true"
      type: query
    - name: ordenar_por
      value: valoracion
      type: query
    - name: orden
      value: desc
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
