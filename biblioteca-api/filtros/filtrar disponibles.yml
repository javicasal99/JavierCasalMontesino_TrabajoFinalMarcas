info:
  name: filtrar disponibles
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/libros/buscar?disponible=true
  params:
    - name: disponible
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
